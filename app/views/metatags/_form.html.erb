<%= form_for(@metatag) do |f| %>
  <% if @metatag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@metatag.errors.count, "error") %> prohibited this metatag from being saved:</h2>

      <ul>
      <% @metatag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tag %><br />
    <%= f.text_field :tag %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description, rows: 10 %>
  </div>
  <div class="field">
    <%= f.check_box :user_appliable %>
    <%= f.label :user_appliable %><br />
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
